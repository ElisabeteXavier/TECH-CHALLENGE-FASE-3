# --- Assistente (gerar_dados.py, rag, agentes, grafo) ---
python-dotenv>=1.0.0
openai>=1.30.0
reportlab>=4.0.0

# --- LangChain / LangGraph (multi-agentes) ---
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-huggingface>=0.1.0
langchain-text-splitters>=0.3.0
langgraph>=0.2.0

# --- RAG (embeddings + PDF + FAISS) ---
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0
pypdf>=4.0.0

# --- EDA / PubMedQA (eda.ipynb) ---
requests>=2.31.0
tqdm>=4.66.0
pandas>=2.0.0
matplotlib>=3.8.0

# --- Fine-tuning (eda.ipynb / colega — opcional, pesado) ---
# Descomente se for treinar localmente ou no Colab com GPU:
# torch>=2.2.0
# transformers>=4.40.0
# peft>=0.11.0
# datasets>=2.19.0
# accelerate>=0.30.0
# huggingface-hub>=0.23.0